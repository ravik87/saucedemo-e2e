const UIContent = {
    CHECKOUT_COMPLETE_MSG: 'Thank you for your order!',
    CHECKOUT_COMPLETE_INFO: 'Your order has been dispatched, ' +
        'and will arrive just as fast as the pony can get there!'
};

Object.freeze(UIContent);

export default UIContent