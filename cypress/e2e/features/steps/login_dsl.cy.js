/// <reference types="cypress" />
import {Given, When, Then, And} from "cypress-cucumber-preprocessor/steps"

Given("I login to Swag Labs UI", (datatable) => {
});

Then("I should see the home page", (datatable) => {
});
